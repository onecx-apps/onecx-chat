apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onecx-chat-svc.fullname" . }}-test-connection"
  labels:
    {{- include "onecx-chat-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onecx-chat-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
